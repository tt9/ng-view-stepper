import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ViewStepperViewComponent } from './view-stepper-view.component';

describe('ViewStepperViewComponent', () => {
  let component: ViewStepperViewComponent;
  let fixture: ComponentFixture<ViewStepperViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ViewStepperViewComponent],
    }).compileComponents();
  }));

  beforeEach(() => {
    // fixture = TestBed.createComponent(ViewStepperViewComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
